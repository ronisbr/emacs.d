;; =============================================================================
;; setup-minimap.el
;;
;;      Configuration file: Minimap.
;;
;; =============================================================================

(use-package minimap
  :config
  (setq minimap-window-location 'right))

(provide 'setup-minimap)
