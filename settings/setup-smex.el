;; =============================================================================
;; setup-smex.el
;;
;;      Configuration file: smex.
;;
;; =============================================================================

(use-package smex
  :bind ("M-x" . smex))

(provide 'setup-smex)
